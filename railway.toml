[build]
builder = "nixpacks"

[build.env]
NIXPACKS_BUILD_CMD = "pip install -r requirements_cloud.txt"

[deploy]
startCommand = "uvicorn nexus_server:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
